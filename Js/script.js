// Inicialização do Script
document.addEventListener('DOMContentLoaded', () => {
    initApp();
});

function initApp() {
    console.log("DOM carregado. Pronto para desenvolver.");
    // Adicione suas funções aqui
}